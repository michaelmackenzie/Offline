#include "Mu2eG4/geom/geom_common_cd3_s4p2.txt"
int stm.verbosityLevel = 1;
int ts.coll.verbosityLevel = 1;
//---------- Set STM configuration for this study ----------
string stm.shield.material = "StainlessSteel";

//"absorber" , just upstream of the "spot-size" collimator //
bool   stm.absorber.build             =  false; //
string stm.absorber.material          = "G4_Pb";//Polyethylene096
double stm.absorber.DnStrSpace        = 1500.0; // this parameter not used for this collimator
double stm.absorber.halfWidth         =  215.9; // 
double stm.absorber.halfHeight        =  228.6; // 
double stm.absorber.halfLength=5; // 76.2mm = 3 inch halflength 

//---------- Set VD configuration for this study -----------
int    vd.verbosityLevel            = 1;
bool   vd.STMUpStr.build            = true; //VD86 Rectangle in x-y plane, same size as the CRV
bool   vd.STMMagDnStr.build         = true; //VD87 Rectangle in x-y plane, same size as the CRV
bool   vd.STMFin.build         = true; //VD87 Rectangle in x-y plane, same size as the CRV
bool   vd.STMMid.build         = true; //VD87 Rectangle in x-y plane, same size as the CRV
bool   vd.STMCollDnStr.build        = true; //VD88
bool   vd.STMDet1UpStr.build        = true; //VD89 Disk in x-y plane, same size as the Ge detector 
bool   vd.STMDet2UpStr.build        = true; //VD90 Disk in x-y plane, same size as the LaBr3 detector 
bool   vd.DSNeutronShieldExit.build = true;
bool   vd.STMFieldOfViewCollDnStr.build = true; //VD100
bool   vd.STMSSCollUpStr.build = true; //VD101 Disk upstream of Spot-Size collimator

bool   stm.det1.build=false;
bool   stm.det2.build=false;

//shield pipe to reduce CRV deadtime
bool   stm.SScollimator.hole1.liner1.build      =false; //wyq liner along the length of the hole itself
double stm.SScollimator.hole1.liner1.thickness  =  40.0;
bool   stm.SScollimator.hole1.liner2.build      =  false; //wyq liner along the length of the hole itself
double stm.SScollimator.hole1.liner2.thickness  =  0.4; //wyq  0.04 cm thick hole liner
bool   stm.SScollimator.hole2.liner1.build      =  false; //wyq liner along the length of the hole itself
double stm.SScollimator.hole2.liner1.thickness  =  40.0; //wyq 1cm thick hole liner
bool   stm.SScollimator.hole2.liner2.build      =  false; //wyq liner along the length of the hole itself
double stm.SScollimator.hole2.liner2.thickness  =  0.4; //wyq 0.04cm thick hole liner
//string stm.FOVcollimator.absorber.material      =  "DSVacuum";
//string ifb.endwindow.material   = "DSVacuum";
//double stm.FOVcollimator.absorber.halfLength=0.0001;//default is 5
//double ifb.endwindow.halfLength = 0.0001;//default is also 5
 

// vim: syntax=c
// This tells emacs to view this file in c++ mode.
// Local Variables:
// mode:c++
// End:
